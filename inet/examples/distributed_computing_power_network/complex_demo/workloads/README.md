# Local workloads

Place generated or large-scale workload JSON files in this directory.

Files matching `*.json` are intentionally ignored by Git. Keep committed examples as `*.example.json`, or point `taskFile` to a local file such as `workloads/tasks.local.json` from a local ini override.
